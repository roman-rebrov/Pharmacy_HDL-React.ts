import { State } from "../Types/types";
import InitialState from "./initialState";

const selectedReducer = (state : State = InitialState) => {
    let selectedKeys : string[] = [];

    return (state)
}

export default selectedReducer;